# Zain Xd — Simple Flask Website

## Local Run
- python -m venv venv
- source venv/bin/activate
- pip install -r requirements.txt
- python app.py

## Deployment
Use Render or Heroku with command:
gunicorn app:app
